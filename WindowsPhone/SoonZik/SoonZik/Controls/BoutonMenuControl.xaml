<UserControl
    x:Class="SoonZik.Controls.BoutonMenuControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoonZik.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{Binding RelativeSource={RelativeSource Self}}"
    d:DesignHeight="40"
    d:DesignWidth="220">
    
    <Grid Tapped="UIElement_OnTapped">
        <Canvas Width="220" Height="40" Canvas.Top="66">
            <Image Source="{Binding Image}" Width="30" Height="30" Canvas.Left="10" Canvas.Top="4"/>
            <TextBlock Text="{Binding Title}" FontFamily="Calbri" FontSize="20" Canvas.Left="75" Canvas.Top="10"/>
        </Canvas>
    </Grid>
</UserControl>
