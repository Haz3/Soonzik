<Page
    x:Class="SoonZik.Views.News"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    DataContext="{StaticResource NewsViewModel}"
    Background="#424242">

    <Page.Resources>
        <DataTemplate x:Key="NewsTemplate">
            <StackPanel Margin="0,0,0,30">
                <TextBlock Text="{Binding Path=Title}" Style="{StaticResource TitleTextBlockStyle}"/>
                <TextBlock Text="{Binding Path=Date}"></TextBlock>
            </StackPanel>
        </DataTemplate>
    </Page.Resources>    
    
    <Grid>
        <ListBox Background="Transparent" x:Name="ListViewNews" ItemsSource="{Binding ListNews}" ItemTemplate="{StaticResource NewsTemplate}">

        </ListBox>
        
        <!--<control:NewsControl Width="380" Height="200" VerticalAlignment="Top" Margin="10,10,10,0"/>-->
    </Grid>
</Page>

