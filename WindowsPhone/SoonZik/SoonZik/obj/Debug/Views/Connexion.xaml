<Page
    x:Class="SoonZik.Views.Connexion"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoonZik.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    xmlns:facebookControls="clr-namespace:Facebook.Client.Controls;assembly=Facebook.Client"
    xmlns:controls="using:Facebook.Client.Controls"
    DataContext="{StaticResource ConnexionViewModel}"
    Background="#D3D3D3">

    <Grid x:Name="GlobalGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="70"/>
            <RowDefinition Height="500"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Row 1 -->
        <TextBlock x:Name="TitleBlock" Text="Bienvenue" FontFamily="{StaticResource Roboto}" FontSize="50" Foreground="Black" Grid.Row="0"/>

        <!-- Row 2 -->
        <Image Grid.Row="1" Source="../Resources/logo.png" Width="300" Height="150" VerticalAlignment="Top"/>
        <TextBox x:Name="UserTextBox" PlaceholderText="Username" Text="{Binding Username, Mode=TwoWay}" Grid.Row="1" Background="LightGray" Margin="50,318,50,0"/>
        <PasswordBox x:Name="PasswordTextBox" PlaceholderText="Password" Password="{Binding Password, Mode=TwoWay}" Grid.Row="1" Background="LightGray" Margin="50,381,50,0"/>

        <Button Grid.Row="1" Content="Connexion" BorderThickness="0" Foreground="Black" Margin="250,415,0,28" Command="{Binding ConnexionCommand}"/>

        <!-- login control -->

        <!-- Row 3 -->
        <Button Grid.Row="2" Content="Inscription" Foreground="Black" BorderThickness="0" Margin="18,7,0,6"/>
        <Button Grid.Row="2" Content="Forgot Password" Foreground="Black" BorderThickness="0" Margin="229,7,0,6"/>
    </Grid>
</Page>

