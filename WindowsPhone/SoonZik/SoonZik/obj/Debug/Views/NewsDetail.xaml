<Page
    x:Class="SoonZik.Views.NewsDetail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoonZik.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{StaticResource NewsDetailViewModel}">

    <Grid x:Name="GridDetailNews" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="150"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        
        <Grid Grid.Row="0">
            <TextBlock Foreground="White" Text="{Binding SelectNews.Title}" VerticalAlignment="Top" HorizontalAlignment="Center" FontSize="30"/>
        </Grid>
        <Grid Grid.Row="1">
                <TextBlock Foreground="White" VerticalAlignment="Bottom" HorizontalAlignment="Left" FontSize="15" Text="{Binding SelectNews.User.username}"/>
                <TextBlock Foreground="White" VerticalAlignment="Bottom" HorizontalAlignment="Right" FontSize="15" Text="{Binding SelectNews.Date}"/>
        </Grid>
    </Grid>
</Page>

