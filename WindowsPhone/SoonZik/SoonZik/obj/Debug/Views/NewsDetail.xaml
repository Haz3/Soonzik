<Page
    x:Class="SoonZik.Views.NewsDetail"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoonZik.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}"
    DataContext="{StaticResource NewsDetailViewModel}">

    <Grid x:Name="GridDetailNews" Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="150" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Image Height="100" VerticalAlignment="Top" Source="{Binding SelectNews.attachments[0].url}"/>
            <TextBlock Foreground="White" Text="{Binding NewsTitle}" VerticalAlignment="Bottom"
                       HorizontalAlignment="Center" FontSize="30" />
        </Grid>
        <Grid Grid.Row="1">

            <TextBlock Foreground="White" VerticalAlignment="Top" FontSize="15" Text="{Binding NewsContent}" 
                       TextWrapping="Wrap"/>

            <TextBlock Foreground="White" VerticalAlignment="Bottom" HorizontalAlignment="Left" FontSize="15"
                       Text="{Binding SelectNews.user.username}" />
            <TextBlock Foreground="White" VerticalAlignment="Bottom" HorizontalAlignment="Right" FontSize="15"
                       Text="{Binding SelectNews.created_at}" />
        </Grid>
    </Grid>
</Page>

