<Page
    x:Class="SoonZik.Views.ProfilArtiste"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:SoonZik.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:interactivity="using:Microsoft.Xaml.Interactivity"
    xmlns:core="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    DataContext="{StaticResource ProfilArtisteViewModel}"
    Background="#424242">

    <interactivity:Interaction.Behaviors>
        <core:EventTriggerBehavior EventName="Loaded">
            <core:InvokeCommandAction Command="{Binding SelectionCommand}" />
        </core:EventTriggerBehavior>
    </interactivity:Interaction.Behaviors>


    <Grid x:Name="GlobalGrid">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="250" />
        </Grid.RowDefinitions>
        <Grid.Resources>
            <DataTemplate x:Key="AlbumTemplate">
                <Grid x:ConnectionId='1' Height="70" Margin="20,0,0,0"                            >
                    <Border BorderThickness="0,0,0,1" Width="350" BorderBrush="White" />
                    <Image x:Name="AlbumImage" Source="../Resources/Icones/MenuExplorer.png" Height="50"
                           VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10,0,0,0" />
                    <TextBlock x:Name="MusicTitle" Text="{Binding Path=title}" FontSize="25"
                               HorizontalAlignment="Center" />
                    <Image x:Name="MoreOption" Source="../Resources/Icones/menu.png" Height="30"
                           VerticalAlignment="Center" HorizontalAlignment="Right" Margin="0,0,20,0" />
                </Grid>
            </DataTemplate>
        </Grid.Resources>

        <Grid Grid.Row="0">
            <Image Name="ImageArtiste" Source="{Binding TheArtiste.profilImage}" Width="200" Height="200"
                   HorizontalAlignment="Center" VerticalAlignment="Top"
                   Margin="0,30,0,0" />

            <TextBlock x:Name="AlbumTitle" Text="{Binding TheArtiste.username}" FontSize="30"
                       VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,120,0,0" />

            <TextBlock x:Name="ArtistName" Text="{Binding NbrFollowers}" FontSize="20"
                       VerticalAlignment="Center" HorizontalAlignment="Center" Margin="0,250,0,0" />
            <Button VerticalAlignment="Bottom" HorizontalAlignment="Left" Content="{Binding FollowText}"
                    Command="{Binding FollowCommand}" />
            <Button Grid.Row="0" VerticalAlignment="Bottom" HorizontalAlignment="Right"
                    Content="{Binding ButtonFriendText}"
                    Command="{Binding AddCommand}" />
        </Grid>
        <ListView Grid.Row="1" x:Name="MusicListView" ItemsSource="{Binding ListAlbums}"
                  ItemTemplate="{StaticResource AlbumTemplate}" SelectedItem="{Binding TheAlbum, Mode=TwoWay}" />
    </Grid>


</Page>
<!--<Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="18*"/>
            <RowDefinition Height="30*"/>
        </Grid.RowDefinitions>

        -->
<!-- Row 1 -->
<!--<Grid Grid.Row="0" Background="#d3d7da">
            <Image Source="../Resources/logo.png" Height="100" Width="150" Grid.Row="0" VerticalAlignment="Center"/>
        </Grid>-->
<!-- Row 2 -->
<!--
        <Grid Grid.Row="0" Background="Transparent">
            <Image Source="../Resources/Icones/disc.png" Height="160" Width="160" HorizontalAlignment="Left" Margin="20,0,0,0"/>
            <TextBlock Text="Artist Name" FontFamily="Calibri" FontSize="30" HorizontalAlignment="Right" Margin="0,38,49,42"/>
        </Grid>
        -->
<!-- Row 3 -->
<!--<Grid Grid.Row="1" Background="Transparent">
            <Pivot x:Name="PivotProfil" HeaderTemplate="{StaticResource SizeHeaderGlobalPivot}" Style="{StaticResource PivotStyle1}">
                <PivotItem Header="Bio">
                    <ScrollViewer>
                        <StackPanel>
                            <TextBlock FontSize="20" FontFamily="Calibri" TextWrapping="Wrap">
                                <TextBlock.Text>
                                    Un texte est donc écrit, avant d'être lu. 
                                    Il n'y a pas de limitation dans la longueur d'un texte. Du fait de la syntaxe de la plupart des langages, un texte s'organise généralement en une suite de mots et de phrases.
                                    La définition de texte ci-dessus désigne une sémiotique du langage (proprement acte sémique) qui en tant que tel transcende l'acte textuel. Il en résulte que tout texte peut se transposer dans n'importe quelle langue écrite, au travers d'un processus de traduction, aux dépens cependant d'une perte partielle de sens selon la qualité de la traduction et la proximité des langages.
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </ScrollViewer>
                </PivotItem>
                <PivotItem Header="Discographie">
                    <ScrollViewer>
                        <StackPanel>
                            <TextBlock FontSize="20" FontFamily="Calibri" TextWrapping="Wrap">
                                <TextBlock.Text>
                                    Un texte est donc écrit, avant d'être lu. 
                                    Il n'y a pas de limitation dans la longueur d'un texte. Du fait de la syntaxe de la plupart des langages, un texte s'organise généralement en une suite de mots et de phrases.
                                    La définition de texte ci-dessus désigne une sémiotique du langage (proprement acte sémique) qui en tant que tel transcende l'acte textuel. Il en résulte que tout texte peut se transposer dans n'importe quelle langue écrite, au travers d'un processus de traduction, aux dépens cependant d'une perte partielle de sens selon la qualité de la traduction et la proximité des langages.
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </ScrollViewer>
                </PivotItem>
                <PivotItem Header="Social">
                    <ScrollViewer>
                        <StackPanel>
                            <TextBlock FontSize="20" FontFamily="Calibri" TextWrapping="Wrap">
                                <TextBlock.Text>
                                    Un texte est donc écrit, avant d'être lu. 
                                    Il n'y a pas de limitation dans la longueur d'un texte. Du fait de la syntaxe de la plupart des langages, un texte s'organise généralement en une suite de mots et de phrases.
                                    La définition de texte ci-dessus désigne une sémiotique du langage (proprement acte sémique) qui en tant que tel transcende l'acte textuel. Il en résulte que tout texte peut se transposer dans n'importe quelle langue écrite, au travers d'un processus de traduction, aux dépens cependant d'une perte partielle de sens selon la qualité de la traduction et la proximité des langages.
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </ScrollViewer>
                </PivotItem>
                <PivotItem Header="A écouter">
                    <ScrollViewer>
                        <StackPanel>
                            <TextBlock FontSize="20" FontFamily="Calibri" TextWrapping="Wrap">
                                <TextBlock.Text>
                                    Un texte est donc écrit, avant d'être lu. 
                                    Il n'y a pas de limitation dans la longueur d'un texte. Du fait de la syntaxe de la plupart des langages, un texte s'organise généralement en une suite de mots et de phrases.
                                    La définition de texte ci-dessus désigne une sémiotique du langage (proprement acte sémique) qui en tant que tel transcende l'acte textuel. Il en résulte que tout texte peut se transposer dans n'importe quelle langue écrite, au travers d'un processus de traduction, aux dépens cependant d'une perte partielle de sens selon la qualité de la traduction et la proximité des langages.
                                </TextBlock.Text>
                            </TextBlock>
                        </StackPanel>
                    </ScrollViewer>
                </PivotItem>
            </Pivot>
        </Grid>-->
<!-- Row 4 -->
<!--
    </Grid>-->

