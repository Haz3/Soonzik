.row
  %h1
    Listing news
    - if @news.nil? && defined?@news
      %table
      %tr
        %th Title
        %th Date
        %th Author
        %th
        %th
        %th
      - @news.each do |news|
        %tr
          %td= news.title
          %td= news.date
          %td= news.author_id
          %td= link_to 'Show', news
          %td= link_to 'Edit', edit_news_path(news)
          %td= link_to 'Destroy', news, :method => :delete, :data => { :confirm => 'Are you sure?' }
    - else
      %h3.subheader
        Il n'y a pas de News à Afficher
%br

= link_to 'New News', new_news_path
